<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1> random number: <%=random %></h1>

<% if (random % 2 === 0) { %>
    <h3>Angka genap</h3>
<% } else {  %>
     <h3>Angka ganjil</h3>
<% } %>     

<h1> That number:   <%= random % 2 === 0 ? 'even' : 'odd' %></h1>
</body>
</html>